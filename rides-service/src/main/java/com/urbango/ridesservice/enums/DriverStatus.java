package com.urbango.ridesservice.enums;

// Réplica del Enum definido en drivers-service
public enum DriverStatus {
    PENDING_APPROVAL,
    ACTIVE_OFFLINE,
    ACTIVE_AVAILABLE,
    ON_RIDE,
    SUSPENDED,
    REJECTED
}
