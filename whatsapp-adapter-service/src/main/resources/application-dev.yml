server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/whathaAppAdapter_service_dev
    username: postgres
    password: sasa
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
